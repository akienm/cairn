{
  "component": "cairn-command",
  "role": "charter",
  "schema_version": "v0 — component-charter shape; migrates into the intention envelope when MAP.md Q6 lands",
  "what": "THE USER-FACING COMMAND RUNNER: a `bin/` at the repo root with a single dumb dispatcher, `cairn`, so Akien invokes any Cairn facility by `cairn <name> <args...>` — which resolves to `bin/cmd/<name>` and execs it, argv verbatim, exit code intact. UU's `uu $1 $2 ...` pattern, Cairn's name. The dispatcher owns NO logic of what it runs (Law 6 — each facility owns itself); it resolves a name and hands off, fails LOUD on an unknown name (CP1), and adds nothing to stdout on the success path. A new verb is a new executable in `bin/cmd/` — no edit to the dispatcher. First verb: `bin/cmd/compile` (the intentions-model-compiler's hand-crank). The full name `cairn` lives in git; any 2-letter shorthand is a PERSONAL shell alias in dotfiles (instance-space), never in this shared tree.",
  "why": "Founding intention — get Akien out of hand-wiring the system: a named front door beats a remembered path, and it is the seam every later `cairn <verb>` (compile, scan, intent, ...) hangs off. Putting the FULL name in git and leaving the cramped shorthand to a personal alias keeps the class/instance line honest: the durable shared thing is the self-documenting name (`cairn compile` reads in any ticket or doc); the fingers-preference form is per-instance personal data. Minimalism (Law 1): the dispatcher is the smallest thing that resolves-and-hands-off — resolution smarts grow against a real need, not before.",
  "invoke": "Run: `bin/cairn <name> <args...>` (or `cairn <name> ...` once bin/ is on PATH — see host_seam). No args or an unknown name prints the command list and fails non-zero. Add a verb: drop an executable at `bin/cmd/<name>`. Override the command dir (for the proof): `CAIRN_CMD_DIR=<dir>`. Prove: `python3 bin/proofs/test_cairn_command.py` (exit 0 = green).",
  "node_class": "code-seam — the dispatcher script + bin/cmd/ tree are the code (git-tracked, one address). Carries a host-seam EDGE: being callable as bare `cairn` from anywhere needs bin/ on PATH, which lives where git cannot see it. This is ticket cairn-command.",
  "host_seam": {
    "what": "making `cairn` resolve from any directory — the PATH wiring, host-side, not in git",
    "apply": "add the repo's bin/ to PATH, e.g. append to ~/.bashrc: `export PATH=\"$HOME/dev/src/cairn/bin:$PATH\"` (or a drop-in under /etc/prof.d). A personal shorthand is optional and also host-side: `alias cn=cairn`.",
    "verify": "`command -v cairn` resolves into <repo>/bin/cairn, and `cairn compile` runs. Re-run after any shell/profile change — the seal expires when the host drifts (nothing in git changes).",
    "state": "IOU — not yet applied on this host; bin/cairn is invoked by relative path today. Apply when bare `cairn` is wanted from outside the repo."
  },
  "entry": "FRESH build 2026-07-23. The metaphor is quarried from UU (a `uu` script-prefix dispatcher — `$UU_HOME/scripts/$1 $2 ...`); the shape is Cairn's: a dumb name-resolver that owns nothing, loud on the unknown, argv+exit-code transparent, with the full name in git and the shorthand in instance-space. What is NEW: the class/instance split of the name itself (the design decision the /intent's prefix question resolved into), and the exit-code-transparency contract (a subcommand's failure IS the dispatcher's failure).",
  "state_and_history": "compiled to state.json (projected window) + history.json (append-only voyage) beside this charter — the charter-state-history split; this file stays pure intention+why, the operational voyage lives in the sidecars (Law 1/4/7)",
  "filed_edges": [
    "(a) the flat bin/cmd/<name> passthrough has no subcommand tree / arg-routing — grows against the first facility that needs it",
    "(b) the PATH host-seam is not yet applied on this host (invoked by relative path today)",
    "(c) the charter is not yet gathered by the compiler — gather scans cairn/*/ and a repo-root bin/ charter is outside that scan, closed when seed-claim coverage demands it",
    "(d) slate, turnscan and askscan are the third, fourth and fifth bin-resident facilities under this one charter (the flight_recorder precedent, stretched three times now — twice on 2026-07-31 by the cairnmap derivation gate's first live reds, again on 2026-08-14); each owes its own charter the moment it grows its own voyage"
  ],
  "gates": "proof gate — proofs/test_cairn_command.py, runnable bare (python3) and under the tester. Host-seam gate: the apply/verify recipe above (re-runnable; the seal expires on host drift). Durability gate: not done until proven AND the VALIDATION is persisted beside-code AND committed (git).",
  "falsifier": "RED, by physics, on any of: (1) argv is not passed verbatim (an arg is dropped, re-split, or reordered); (2) the subcommand's exit code is NOT the dispatcher's exit code (a non-zero swallowed — the exact bug found at build); (3) an unknown name does not fail loud (a silent no-op, or a zero exit — CP1 broken); (4) the dispatcher adds output of its own on the success path (it became a wrapper, not a pass-through); (5) the dispatcher grows knowledge of WHAT a subcommand does (special-casing a facility — Law 6, it must only resolve-and-hand-off); (6) the 2-letter shorthand reappears baked into the git tree (an instance concern crossing into class-space).",
  "how_it_learns": "Yes — narrowly, and it says so. The dispatcher itself does not learn (it is a dumb resolver, by design); what LEARNS is the command SET it exposes — every `bin/cmd/<name>` that appears is evidence of a facility Akien reached for often enough to name, and the growth of that directory over time is the record of which routes earned a front door. The dispatcher's own horizon (flat passthrough vs a subcommand tree) firms up as that set grows and a real facility needs argument-routing — a guess-with-an-expiry, not a settled shape. TRACE-WIRED 2026-08-01 (deploy pass, Akien-approved): logger_for_bash's write door now rides learning_block's trace wire, once per sourced shell — the first successful write traces door_pass, an unwritable target traces send_back naming the target, backgrounded and never fatal (the never-abort law extends to the wire). The logger's firings thereby join the system-level denominator: the whole system is the Learning Block (I-the-system-is-a-learning-block), and this is one of its first four counted doors. 2026-08-01 (ticket recordverdict-cli-door): the session-open program grew the AT-YOUR-GATE lane — findings awaiting Akien's verdict print above the slate (pendingness owned by learning_block, rendered here; zero pending prints nothing; a broken berth is loud and never wedges), and the receipt counts what stands at his gate. bin/cmd gained `recordverdict`, a thin exec into the primitive's shell door — no logic, no second writer.",
  "traces_to": [
    "founding intention (get Akien out of running the system — a named route, not a memorized path)",
    "Law 1 (the resolver is not spent re-typing settled paths — the route is compiled into a name; the dispatcher is the minimal thing that resolves-and-hands-off)",
    "Law 6 (the dispatcher owns nothing it runs — it resolves a name and hands off to that facility's own gate)",
    "the roots table (class-space is shareable/git; the ergonomic shorthand is instance-space personal data, never in git)",
    "quarried: UU's `uu $1 $2 ...` script-prefix dispatcher",
    "ticket cairn-command"
  ],
  "owner": "CC owns the cairn-command code (bin/cairn + bin/cmd/) + its proof and gates writes to them. The dispatcher owns no facility state — each subcommand's behavior is owned by that facility (Law 6). The PATH host-seam is owned host-side (instance-space), applied and verified by its recipe, never in git.",
  "proof": "proofs/test_cairn_command.py — four teeth a hollow dispatcher could not pass: args reach the subcommand verbatim (a space-bearing arg not re-split); the subcommand's exit code propagates; an unknown name fails loud with a legible message; no wrapper noise on the success path. Uses CAIRN_CMD_DIR to point at a temp stub tree so it never touches the real bin/cmd/. Green bare (python3) and under the tester; VALIDATION persisted beside-code at validations/test_cairn_command.json. proofs/test_logger_for_bash.py — 18 teeth for the flight recorder, defect-first: the recorded code is the command's not the pipe's, stderr reaches the file, the record keeps argument boundaries, sourcing neither truncates nor prints nor leaks a `set`, trim keeps the newest N and leaves evidence of what it discarded, a multi-line message is still one record, concurrent shells keep all their records, a child renames the namespace without touching the parent, and the install is idempotent and reversible. Non-vacuity: test_the_original_defects_are_real runs the GRAFT SOURCE and asserts it still fails the same fixtures (INDETERMINATE, never a quiet green, where that source is absent).",
  "provenance": "Born 2026-07-23, cast (ticket cairn-command) and built the same session, from an /intent that bundled a command-runner + an intentions updater + a 'repo-scanner'. The prefix question (cc/cs/cr/ccs) resolved by MEASURING PATH conflicts (only `cc` taken — the C compiler, which already means CC-the-mind) and by placing the ergonomic shorthand on the correct side of the class/instance line: full `cairn` in git, personal alias in dotfiles. Akien: 'i thought we'd already decided cairn as the name'; 'let's not do a shortening at this time. cairn is not very long.' The set -e exit-code bug was found by the proof at build and fixed before the seal.",
  "flight_recorder": {
    "what": "bin/logger_for_bash — a SOURCEABLE bash library (not a verb; it lives here to be PATH-adjacent). `source logger_for_bash`, then `logrun <cmd>` runs it and records what it tried, everything it printed on stdout AND stderr, and its real exit code, into one append-only namespace capped at the last N lines.",
    "why_it_is_here_and_not_a_verb": "It is sourced, not dispatched — there is nothing for bin/cmd/ to exec. It sits in bin/ because bin/ is already on PATH, which is the whole reason `. logger_for_bash` works from anywhere. That makes this the SECOND facility under one charter (the same shape as launchers/bootstrap.sh beside superclaude, and the tester's verb beside the device); acceptable while both are PATH-resident tooling of one node_class, and filed as an edge on logger-for-bash if it grows its own voyage.",
    "the_exit_code_is_the_point": "The graft source recorded TEE's exit code, not the command's — `logcmd false` wrote result_code=0, measured 2026-07-30. A log that reports success for everything is worse than no log, because it is believed. ${PIPESTATUS[0]} is the fix and test_the_recorded_code_is_the_commands_not_the_pipes is what keeps it fixed.",
    "one_record_is_one_line": "Every line carries <yyyymmdd.hhmmss.uuuuuu>.<pid>, and embedded newlines collapse at the write door. Many shells append to ONE namespace, so an unprefixed line is unattributable — you cannot tell whose failure you are reading, which is the moment the log had one job. It also makes the line cap mean what it says, and `grep '\\.<pid>:'` reconstruct one session out of the braid.",
    "it_can_never_break_the_caller": "No `set -e`, no `set -u`, nothing printed on source, no truncation on source, and an unwritable target warns ONCE to stderr and returns. It is sourced by ~/.bashrc and by superclaude, so a logger that can abort is a logger that costs you a shell or a launch — the exact inverse of the rescue ethos.",
    "namespaces_are_just_export": "A child process that exports CAIRN_LOGTARGET gets its own namespace for its own lifetime and leaves the parent's alone — no mechanism needed, which is why there isn't one. A SOURCED file has to save and restore instead, and bootstrap.sh's _cairn_boot_log demonstrates that pattern.",
    "cost": "A stamp costs ZERO forks (EPOCHREALTIME + printf %()T, both builtins; `date +%6N` would fork per line). Trim is amortised — the file drifts 25% over the cap and is then cut back, so the rewrite runs once per ~250 lines, fired by the write and never by a clock.",
    "host_seam": "cairn_logger_install / cairn_logger_installed / cairn_logger_uninstall — a delimited, idempotent ~/.bashrc block that backs up first. Git cannot see ~/.bashrc, so this carries apply+verify and its seal expires. NOT APPLIED on this box yet (filed edge)."
  },
  "the_watch": {
    "berth": "bin/probes/retention_window.py — the WATCHME ticket logger-for-bash carries, berthed beside the code it watches (cairn/tools/base/probe.py's rule: a probe berths with its subject, not with the ticket it was compiled from).",
    "question": "Is 1000 lines a window that still explains anything? Akien named the number before any traffic existed to size it against, which makes it a hypothesis wearing a default's clothes. The proofs pin that the window is BOUNDED; no proof can say whether it is BIG ENOUGH, because that needs time.",
    "measure": "Can the record still show TWO RUNS? A namespace that has turned over and now holds one pid has stopped being a record and become a peephole onto the present — which is the one thing the process could already see. Sharper than 'how many hours does it span', and it needs no clock.",
    "proof": "bin/proofs/test_retention_window_probe.py — the predicates exhausted (no state where it clears but could never have fired), the reader proved against the recorder's real grammar, and armed read through the emission gate's own instrument rather than a parallel notion of armed.",
    "what_its_first_live_fire_found": "A pre-image parked beside the record read as a second namespace with 54 runs of its own — a copy of the evidence counted as more evidence. Fixed by physics: anything KEPT rather than WRITTEN lives in a subdirectory, and a subdirectory is not a file."
  },
  "session_open_program": {
    "what": "THE SESSION-OPEN PROGRAM (bin/cmd/slate) — prints the resumption point: live troubles FIRST, then the newest slate's three ratified fields (at_sea, next_direction, open_threads), newest by the slate's own date tiebroken by filename, never mtime. Fired unconditionally by the checked-in SessionStart hook; --hook wraps the same bytes in JSON and pays Akien a one-line receipt naming the slate id and live-trouble count — the two values that move.",
    "why": "Replaced /loadslate 2026-07-31: the first link of the workflow chain had fired only when Akien remembered to type it — discipline doing physics' job (Law 4). Bin-resident under this charter by the flight_recorder precedent.",
    "invoke": "`cairn slate` (plain text, the 9am diagnostic) · `cairn slate --hook` (the SessionStart JSON shape). Prove: `python3 bin/proofs/test_slate.py` (exit 0 = green).",
    "for_whom": "Two users, split by verb: the plain render is FOR AKIEN — his on-demand look at the current slate, any hour, not just session-open (his 2026-08-01 reading, confirmed). `--hook` is for a machine: the SessionStart hook is its only caller."
  },
  "turn_shape_scan": {
    "what": "THE TURN-SHAPE SCAN (bin/cmd/turnscan) — the Stop-hook scan for the two-places rule: something goes wrong and it goes to exactly one of fixed-now or a-ticket, never raised-and-undispositioned. It detects a missing disposition and NAMES the gap without blocking — it cannot see which of the two is owed — and exits 0 on every path, including a crash.",
    "why": "The CLAUDE.md turn-shape IOU's built half, measured before it was built: 466 real turns, 6 reds, 1.3%. Bin-resident under this charter by the flight_recorder precedent.",
    "invoke": "`cairn turnscan` with the Stop-hook payload on stdin (wired directly in .claude/settings.json — hook mode is the default, no flag) · `cairn turnscan --corpus` (re-measure the rate over the real transcripts). Prove: `python3 bin/proofs/test_turnscan.py` (exit 0 = green).",
    "for_whom": "A MACHINE user: the Stop hook is the only caller in the normal course — no human types this. `--corpus` is for whoever re-measures the red rate (CC, historically). NOT a harbor viewer. This field exists because on 2026-08-01 the command's owner could not say from its name who it was for ('i suspect turnscan is so i can see the harbor. maybe. not sure') — the exact gap a for_whom line closes."
  },
  "prebuild_feedback_scan": {
    "what": "THE PREBUILD-FEEDBACK SCAN (bin/cmd/askscan) — the Stop-hook scan for the signal Akien named as the highest-value change: a CLARIFYING question asked AFTER the prebuild chain has run means the chain did not carry the right information in. Two conditions, both required — a clarifying question closing the turn, and a prebuild packet on disk inside the window — and the second is what makes the first evidence rather than punctuation. Sequencing questions are SUBTRACTED by name, because ordering is Akien's call and reding it would punish the one kind of question the system wants asked. It appends the gap to a ledger beside the run that leaked it, prints a receipt, and NEVER blocks.",
    "why": "Akien, 2026-08-13: \"if you're asking me clarifying questions AFTER prebuild has run, that means there wasn't the right info going in and that does need to become feedback.\" RULED 2026-08-14, and it fixes the shape: \"it still should not stop the work, but yes, we wanna capture that.\" So capture and receipt, no decision key anywhere — see ruling 2026-08-14-feedback-at-build-time-never-stops-the-work. MEASURED BEFORE BUILT (Law 3), over the whole prebuild era 2026-07-28..2026-08-12: 318 packets, 442 end_turn messages, 73 ending on a question, 29 of those sequencing, 6 red. The two closest — 214s and 284s after a verdict packet — are unmistakable design facts the chain should have carried in. Bin-resident under this charter by the flight_recorder precedent, beside turnscan whose hook shape it follows.",
    "invoke": "`cairn askscan` with the Stop-hook payload on stdin (wired in .claude/settings.json — hook mode is the default, no flag) · `cairn askscan --corpus` (re-measure the rate over the real transcripts — the falsifier, kept runnable) · `cairn askscan --report` (every STANDING gap, beside the packet count that says how many chances there were to leak one, and the retracted count and its corrections; add `--retracted` to list what was retracted) · `cairn askscan --retract <packet.json>` (append a correction row: {question verbatim, reason, authority}; refuses one that matches no standing row). Prove: `python3 bin/proofs/test_askscan.py` (exit 0 = green).",
    "for_whom": "A MACHINE user first: the Stop hook is the only caller in the normal course. Then THE PREBUILD CHAIN ITSELF — the ledger is written into the instance that owns the run, because the chain that leaked the gap is the thing that should learn from it, and a central pile would be a collection nobody needs to see whole. `--report` is for Akien or CC asking what the chain has been missing.",
    "the_floor_not_the_ceiling": "The code stratum: a question shape, a lexical subtraction, and a time window. It cannot tell a design question from a rhetorical one and it never will, and it does not judge whether the question was right to ask. THE WINDOW IS A PROXY AND IS RECORDED AS ONE: \"prebuild ran FOR THIS WORK\" is the real condition, \"a packet was written in the last two hours\" is what disk can answer today, so every row carries gap_seconds and the packet it keyed on and a reader can tighten it after the fact. When a prebuild run carries a session id, this correlation retires in favour of it. · *ticket owed* · AND A SECOND KNOWN FALSE-POSITIVE CLASS, NAMED BUT NOT YET SUBTRACTED: a RELAY of a question already standing on the cast ticket before the chain ran. 2 of the 4 standing rows are that shape — one relaying Akien's own spec question, one relaying the_question_for_akien on ticket a-bulk-move-forwards-itself-from-gits-own-rename-record, written at cast on 2026-08-14, three days before the chain ran. The chain did not fail to carry those in; it carried them, and the close relayed them. The justification for subtracting them is the sequencing one exactly — escalating a spec choice is Akien's call by Law 9 and is the behaviour the system wants. IT IS NOT SUBTRACTED, and deliberately: this instrument measures ME, and tuning it to stop flagging my own behaviour is the plaster reflex the ruling requirement exists to catch. It stands as his call. · *ticket owed*",
    "the_ledger_corrects_itself_by_appending": "MEASURED 2026-08-18, AND IT IS THE INSTRUMENT'S OWN FAILURE, NOT THE SIGNAL'S: 15 of the first 19 rows in the production ledger were written by bin/proofs/test_askscan.py. Case 1 must fire the REAL scan as a subprocess — 'it never blocks' is a claim about the process, not about a function — and until this act that subprocess inherited the caller's HOME, so it resolved BERTHS to the LIVE instance, found the most recent real packet, and appended its fixture question. The rows carried real packet paths, real gap_seconds and a monotonically growing window; they looked exactly like evidence. The falsifier that named it: `wc -l` before and after one proof run, 18 -> 19, plus a grep proving the sentence appears in no transcript as anything anyone said. TWO FIXES, BOTH PHYSICS. (1) _hook() runs under an isolated HOME — instance space is rooted at HOME, so the whole world moves for one env var and no injection seam is added to the code under proof. (2) A tooth takes every production ledger's bytes before case 1 and compares after case 9, so it spans every case; verified able to go red by reverting the isolation, where it named the path and the 537 bytes. AND THE ROWS WERE NOT DELETED. A ledger that can be edited is not a record, so a correction is another ROW — {retracts: {question, before}, reason, authority} — and the reader applies it, exactly like the sequencing subtraction one stratum up: visible, therefore arguable. The door refuses a retraction matching no standing row, because 'these rows are junk' is a claim about disk, and a false one would sit there forever looking like a correction that had been applied. Rows written by the hook now carry `source: \"hook\"`; a row without one predates the stamp and cannot be attributed, which is exactly the state all 19 were in."
  }
}
