# ground_loop — the generic driver-executor: wire a PROVEN method to an OWNED target,
# run it, leave a legible record. Lazy on purpose (boot-order law): the package import
# pulls in nothing heavy; db/tester come in only when a driver actually runs.
